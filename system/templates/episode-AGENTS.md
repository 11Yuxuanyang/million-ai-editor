# 单期剪辑路由

继承仓库根目录 `AGENTS.md` 的母规则与审美系统。

- 主控必须使用 `hyperframe-video-editor` 理解全片、定编辑主线并写完整分镜。
- 只有在 `sequences/<id>/TASK.json` 存在时，才使用 `hyperframe-sequence-worker` 实现该语义段。
- 本期事实以 `BRIEF.md`、`SCRIPT.md`、`SOURCES.md`、`DESIGN.md`、`MOTION-STORYBOARD.md` 和 `work/` 下的当期文件为准。
- 不得用工具通用示例取代本项目的剪辑路由。
