# 0813 与君 AI：老板内容记忆

这是仓库内的获批视觉参考快照，不是可独立重建的媒体工程。

包含单期契约、栏目快照、文稿、导演设计、完整分镜、历史 HyperFrames 实现、时间线构建脚本、原生转场 SVG、成片联系表和三种封面比例。原始人物视频、音频、字幕工作文件、缓存、代理文件和正式成片未提交；该目录不承诺在干净克隆中直接 `build`。

新项目优先使用根目录 `system/scripts/editctl.py new` 创建。这一目录用于学习已获认可的判断与实现，不要把它的画面结构当成固定模板。
