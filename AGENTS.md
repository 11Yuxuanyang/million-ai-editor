# 百万AI剪辑师

## 母

少规则，多判断。
画面先服务理解与情绪，再谈效果。
质量不为速度让路，但可以用并行、代理和缓存减少等待。
创作判断由核心规则与个人审美共同完成；核心规则从反复成立的个人审美中提炼，也随失败反馈删除。

## 路由

- 主控与子智能体统一使用 `GPT-5.6 Sol`。
- 剪辑使用 `hyperframe-video-editor`；复杂镜头按需读取小型模板 Skill。
- V3 中，主控完成全片理解和分镜后，可以把连续语义段落交给多个 `hyperframe-sequence-worker` 在隔离工作区并行实现；只回收各自段落目录，主控独占总时间线、字幕、全片声音和装配。
- 通用机械步骤由 `system/scripts/editctl.py` 执行；AI 负责内容理解、素材关系、cut plan、分镜与画面生成判断。
- 封面交给不继承主对话的独立 `GPT-5.6 Sol` 执行 `auto-cover-imagegen`，默认交付 `4:3` 与 `3:4` 两版；主 Agent 只接收最终路径、锁定文案和审核结论。

## 事实

- 核心规则读 `config/editorial-defaults.json`、`skills/hyperframe-video-editor/references/production-invariants.md` 与 `docs/EDITORIAL-MOTHER.md`。
- 个人审美读 `library/taste/current.json` 指向的偏好、配色、镜头语言与成熟技巧。
- 当前工作以用户指令和单期目录为准；本机 `NOW.md` 只是可选状态便签，不是事实源，不得覆盖当前任务。
- 单期事实、文稿、素材和分镜留在该视频目录，镜头术语读 `references/SHOT-LANGUAGE.md`；不靠聊天记忆或其他 Session 的状态充当事实。

## 工作

- 先理解原文、音频、人物动作与真实素材，先定全片光影与配色，再用可见的空间、动作和人物关系写完整分镜；专业名词只作检索标签。
- 分镜是创作方案，不是门禁表；允许挑战旧规则和模板。
- 获批成片中反复成立的审美可以提炼进核心；失效、重复或妨碍判断的核心规则应合并或退出，不保护旧规则。
- 素材不足时可以检索有许可的真实素材，也可以设计诚实的视觉比喻。
- 连续低清成片完成后，让一个未参与制作的 Sol 结合连续预览检查分镜、时间线和实现代码的一致性；它不代替主 Agent 与用户的真实观看，也不扩展成多层审核或逐帧报告。
- 用户批准连续预览后才输出正式母版。

## 安全

- 未经明确授权不发布、不永久删除。
- 获准清理的文件移入系统回收站。
